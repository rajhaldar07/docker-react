sudo: required

services:
  - docker

before_install:
  - docker build -t rajhaldar/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rajhaldar/docker-react npm run test -- --watchAll=false
